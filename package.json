{
  "name": "lc-auto-withdraw",
  "version": "1.0.0",
  "description": "Lending Club auto withdrawal script",
  "main": "src/Main.js",
  "scripts": {
    "start": "npm run build && node ./target/Main.js",
    "build": "babel src --source-maps inline --out-dir target",
    "watch": "babel src --source-maps inline --watch --out-dir target",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "6.10.3"
  },
  "author": "Rob Graeber",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.5.1",
    "babel-polyfill": "^6.3.14",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-2": "^6.3.13",
    "chromedriver": "^2.29.2",
    "selenium-webdriver": "^3.4.0",
    "tough-cookie": "^2.0.0",
    "winston2": "^1.0.3"
  }
}
